<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
  <a class="navbar-brand" href="/">Space Missions</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarNavDropdown">
    <ul class="navbar-nav">
      <li class="nav-item">
        <a class="nav-link" href="/">Home</a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="/missions">Missions</a>
      </li>
      <li class="nav-item active">
        <a class="nav-link" href="/astronauts">Astronauts <span class="sr-only">(current)</span></a>
      </li>
      <li class="nav-item">
      <a class="nav-link" href="/spacecraft">Spacecraft</a>
      </li>
      <li class="nav-item">
      <a class="nav-link" href="/destinations">Space Destinations</a>
      </li>
      <li class="nav-item">
      <a class="nav-link" href="/countries">Countries</a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="/missions_to_astronauts">Missions to Astronauts</a>
      </li>
      <!--
      <li class="nav-item dropdown">
        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdownMenuLink" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
          Other
        </a>
        <div class="dropdown-menu" aria-labelledby="navbarDropdownMenuLink">
          <a class="dropdown-item" href="#">View Mission/Astronauts</a>
          <a class="dropdown-item" href="#">Another action</a>
          <a class="dropdown-item" href="#">Something else here</a>
        </div>
      </li>
  -->
    </ul>
  </div>
</nav>
<br>
<div class="col-md-8">
    <div class="jumbotron pt-4">
        <div class="container-fluid row">
            <div class="col-md-8"><h2 class="text-secondary">Astronauts</h2></div>
            <div class="col-md-3">
                <div class="dropdown">
                  <button class="btn btn-primary dropdown-toggle float-right" type="button" id="dropdownMenuButton" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                    Display by Country
                  </button>
                  <div class="dropdown-menu" aria-labelledby="dropdownMenuButton">
                        <a class="dropdown-item" href="/astronauts">All</a>
                      {{#each countries}}
                        <a class="dropdown-item" href="/astronauts/{{country_id}}">{{name}}</a>
                      {{/each}}
                  </div>
                </div>
            </div>
            <div class="col-md-1"><button type="button" class="btn btn-primary" data-toggle="modal" data-target="#addModal">Add</button></div>
        </div>

        <hr />

        <table class="table table-striped">
            <thead>
            <tr>
                <th></th>
                <th>First Name</th>
                <th>Last Name</th>
                <th>Birth Date</th>
                <th>Death Date</th>
                <th>Country</th>
                <th>Edit</th>
                <th>Delete</th>
            </tr>
            </thead>
            <tbody id="rowgroup">
                {{#each results}}
                <tr>
                    <td><button class="btn btn-primary btn-sm" type="button" data-toggle="collapse" data-target="#collapse-{{astronaut_id}}" aria-expanded="false" aria-controls="collapse-{{astronaut_it}}">></button></td>
                      <td style="display:none;">{{astronaut_id}}</td>
                      <td>{{first_name}}</td>
                      <td>{{last_name}}</td>
                      <td>{{birth_date}}</td>
                      <td>{{death_date}}</td>
                      <td>{{cname}}</td>
                      <td><button type="button" class="btn btn-primary btn-sm" data-toggle="modal" data-target="#editModal">Edit</button></td>
                      <td><form method="get" action="/astronauts/delete/{{astronaut_id}}"><button type="submit" class="btn btn-primary btn-sm">Delete</button></form></td>
                </tr>
                <tr>
                    <td colspan="8">
                        <div class="collapse" id="collapse-{{astronaut_id}}">
                          <div class="card card-body">
                              <ul id="AM-{{astronaut_id}}">
                                  <!-- many-to-many relationship -->
                              </ul>
                          </div>
                        </div>
                    </td>
                </tr>
                {{/each}}
            </tbody>
        </table>
    </div>
</div>

<!-- ADD MODAL -->
<div class="modal fade" id="addModal" tabindex="-1" role="dialog" aria-labelledby="addModalLabel" aria-hidden="true">
  <div class="modal-dialog" role="document">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title" id="addModalLabel">Add Astronaut</h5>
        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
          <span aria-hidden="true">&times;</span>
        </button>
      </div>
      <div class="modal-body">
        <form id="astronaut-add" name="astronaut-add" method="post" action"/astronauts">
            <div class="form-group">
                  <label for="first_name" class="col-form-label">Fist Name:</label>
                  <input type="text" class="form-control" id="first_name" name="first_name">
            </div>
            <div class="form-group">
                  <label for="last_name" class="col-form-label">Last name:</label>
                  <input type="text" class="form-control" id="last_name" name="last_name">
            </div>
           <div class="form-group">
             <label for="birth_date" class="col-form-label">Birth Date:</label>
             <input type="date" class="form-control" id="birth_date" name="birth_date">
           </div>
           <div class="form-group">
             <label for="death_date" class="col-form-label">Death Date:</label>
             <input type="date" class="form-control" id="death_date" name="death_date">
           </div>
          <div class="form-group">
              <label for="country" class="col-form-label">Country</label>
                  <select class="form-control" id="country_id" name="country_id" form="astronaut-add">
                    {{#each countries}}
                      <option value="{{country_id}}">{{name}}</option>
                    {{/each}}
                </select>
          </div>
        </div>
          <div class="modal-footer">
            <button type="button" class="btn btn-secondary" data-dismiss="modal">Cancel</button>
            <input type="submit" class="btn btn-secondary">
          </div>
        </form>
      </div>
    </div>
  </div>
  <!-- END OF ADD MODAL -->

  <!-- EDIT MODAL -->
  <div class="modal fade" id="editModal" tabindex="-1" role="dialog" aria-labelledby="editModalLabel" aria-hidden="true">
    <div class="modal-dialog" role="document">
      <div class="modal-content">
        <div class="modal-header">
          <h5 class="modal-title" id="editModalLabel">Edit Astronaut</h5>
          <button type="button" class="close" data-dismiss="modal" aria-label="Close">
            <span aria-hidden="true">&times;</span>
          </button>
        </div>
        <div class="modal-body">
          <form id="editRow" name="editRow">
              <input type="hidden" id="edit-id" name="editId">
              <div class="form-group">
                    <label for="edit-name" class="col-form-label">First Name:</label>
                    <input type="text" class="form-control" id="edit-name" name="editName">
              </div>
              <div class="form-group">
                    <label for="edit-name" class="col-form-label">Last Name:</label>
                    <input type="text" class="form-control" id="edit-name" name="editName">
              </div>
             <div class="form-group">
               <label for="edit-date" class="col-form-label">Birth Date:</label>
               <input type="date" class="form-control" id="edit-date" name="editDate">
             </div>
             <div class="form-group">
               <label for="edit-date" class="col-form-label">Death Date:</label>
               <input type="date" class="form-control" id="edit-date" name="editDate">
             </div>
            <div class="form-group">
              <label for="edit-reps" class="col-form-label">Country</label>
              <input type="number" class="form-control" id="edit-reps" name="editReps">
            </div>
          </div>
            <div class="modal-footer">
              <button type="button" class="btn btn-secondary" data-dismiss="modal">Cancel</button>
              <button type="submit" class="btn btn-secondary">Save</button>
            </div>
          </form>
        </div>
      </div>
    </div>
    <!-- END OF EDIT MODAL -->

    <script type="text/javascript">
        var myAPI = 'http://flip1.engr.oregonstate.edu:7798/am';
        loadEvent().then(processLoadEvent, errorHandler);

        function loadEvent() {
            return new Promise(function(resolve, reject) {
                var req = new XMLHttpRequest();
                req.open('GET', myAPI, true);
                req.onreadystatechange = function() {
                    if (req.readyState === 4) {
                        if (req.status === 200) {
                            console.log("req done successfully");
                            resolve(JSON.parse(req.responseText));
                        } else {
                            reject(req.status);
                            console.log("req failed");
                        }
                    } else {
                        console.log("req processing going on");
                    }
                }
                req.send(null);
            });
        }

        function processLoadEvent(elements) {
           setTimeout(function() {
               console.log(elements);
               //Add New Row
               for (var i in elements) {
                   console.log(elements[i]);
                   var myElement = document.getElementById("AM-" + elements[i].astronaut_id);
                   if(myElement){
                        var newLI = document.createElement("li");
                        newLI.innerHTML = "" + elements[i].sname + " to " + elements[i].dname + " from " + elements[i].launch_date + " to " + elements[i].end_date;
                        myElement.appendChild(newLI);
                   }

               }
           }, 0);
        }
        function errorHandler(statusCode){
         console.log("failed with status", status);
        }
    </script>
